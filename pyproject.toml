[project]
name = "piper"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "piper-tts>=1.3,<2",
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.34,<1",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
]

[tool.pytest.ini_options]
pythonpath = ["."]
