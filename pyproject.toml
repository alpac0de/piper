[project]
name = "piper"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "piper-tts>=1.3,<2",
    "flask>=3.0,<4",
    "gunicorn>=21,<23",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
